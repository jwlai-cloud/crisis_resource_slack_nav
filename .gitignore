# Secrets / environment
.env*
!.env.example

# Python
.venv/
venv/
env*/
__pycache__/
*.py[cod]
*.egg
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
dist/
build/

# Logs
logs/

# OAuth local store (app_oauth.py)
data/

# OS / editors
.DS_Store
.idea/

# Claude Code local settings
.claude/*.local.json

# Slack CLI internals (`.slack/` ships its own .gitignore for apps.dev.json etc.)
# Cross-process coordinator-board canvas id (task 018): sandbox-specific state the
# `make board` script + the agent share, not source. The Slack CLI's own
# .slack/.gitignore doesn't cover it, so ignore it here.
.slack/board_canvas_id
